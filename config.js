const ApplicationConfig = {
    runMysql: false,
    host: "localhost",
    backendUrl: "localhost:8080",
    user: 'root',
    password: 'MyNewPass',
    database: 'tfs'
};

export default ApplicationConfig;
